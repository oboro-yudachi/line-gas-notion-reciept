{
  "devDependencies": {
    "@types/google-apps-script": "^2.0.8",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@google/clasp": "^3.2.0",
    "typescript-language-server": "^5.1.3"
  },
  "scripts": {
      "build": "npx tsc && cp src/appsscript.json dist/",
      "push": "npm run build && clasp push",
      "check": "npx tsc --noEmit"
  }
}
